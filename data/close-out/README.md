# Close-Out Data

Nightly close-out reports written by the `close-out` skill.

Each file is named `YYYY-MM-DD.md` and contains:
- Cash reconciliation (cash count vs expected)
- Tip pool split
- Waste/spillage/comp log
- Variance notes

Files here are generated at runtime and gitignored (except this README and template.md).
