# 86 List Data

Current 86 list managed by the `eighty-six` skill.

`current.md` tracks items that are currently unavailable (86'd).
Items are added when stock runs out or a decision is made to pull something.
Items are removed when back in stock.

Files here are generated at runtime and gitignored (except this README).
